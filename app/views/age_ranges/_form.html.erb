<%= form_with(model: age_range, local: true) do |form| %>
  <% if age_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(age_range.errors.count, "error") %> prohibited this age_range from being saved:</h2>

      <ul>
      <% age_range.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
